<!DOCTYPE html>
<html lang="fr">
<head>
    {{content_for_header}}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-mQn+..." crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>{{ page_title }} - {{ shop.name }}</title>
    {{ 'theme.css' | asset_url | stylesheet_tag }}
</head>
<body>
    {% section 'header' %}
    {{ content_for_layout }}
    {% section 'footer' %} 
</body>
</html>